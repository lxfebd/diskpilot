# DiskPilot · 完整使用说明书

> 面向所有使用者的功能说明（v0.2.0）。本说明书覆盖：安装启动、免责声明、磁盘扫描、资产总览、AI 顾问、安全清理、工具墙、基准测试、硬件中心、系统工具、权限中心、MCP 接入、插件市场、清理提醒与全部设置项。
>
> 一句话定位：**DiskPilot 是 Windows 优先的磁盘清理桌面应用**——秒扫整盘（NTFS MFT 直读）、拖文件夹给 AI 问、已知应用走清理脚本，三件事。
>
> 安全底线贯穿全产品：**宁可错放 1000GB，不可错删一个文件。** 所有删除默认进系统回收站，永远不读你的文件内容。

---

## 目录

1. [安装与启动](#一安装与启动)
2. [免责声明（首次启动必看）](#二免责声明首次启动必看)
3. [界面总览：三大视图](#三界面总览三大视图)
4. [磁盘扫描：看清空间都去哪了](#四磁盘扫描看清空间都去哪了)
5. [磁盘资产总览](#五磁盘资产总览)
6. [工作台：目录树与 Treemap](#六工作台目录树与-treemap)
7. [AI 顾问：拖文件夹给 AI 问](#七ai-顾问拖文件夹给-ai-问)
8. [Studio 清理工作台：一条一条删干净](#八studio-清理工作台一条一条删干净)
9. [清理确认与撤销：安全执行链路](#九清理确认与撤销安全执行链路)
10. [工具墙：图吧工具箱集成](#十工具墙图吧工具箱集成)
11. [服务工具行：网络 / 系统信息 / 硬件信息](#十一服务工具行网络-系统信息-硬件信息)
12. [基准测试与压力测试（AIDA64 复刻）](#十二基准测试与压力测试aida64-复刻)
13. [硬件中心：报告与历史对比](#十三硬件中心报告与历史对比)
14. [系统工具：启动项 / 风扇 / 驱动](#十四系统工具启动项-风扇-驱动)
15. [权限中心：四级权限模型](#十五权限中心四级权限模型)
16. [MCP 服务器：接入任意 MCP 服务](#十六mcp-服务器接入任意-mcp-服务)
17. [插件市场：内置插件化 + 社区插件](#十七插件市场内置插件化-社区插件)
18. [清理提醒：后台只读巡检](#十八清理提醒后台只读巡检)
19. [设置：全部八个标签页](#十九设置全部八个标签页)
20. [数据存哪与隐私承诺](#二十数据存哪与隐私承诺)
21. [常见问题（FAQ）](#二十一常见问题faq)
22. [免责声明全文](#二十二免责声明全文)

---

## 一、安装与启动

### 1.1 获取安装包

到 GitHub Releases 下载 Windows 安装包（**发布通道上线前**：先用 `pnpm tauri build` 自行编译，产物在 `apps/desktop/src-tauri/target/release/bundle/`）：

| 格式 | 特点 |
|---|---|
| **NSIS**（.exe） | 单文件安装器，体积较小，安装快 |
| **MSI**（.msi） | 企业/批量部署友好，支持静默安装与系统管理 |

### 1.2 安装

1. 双击安装包 → 按向导下一步（建议保持默认安装路径）。
2. 安装完成后桌面出现 **DiskPilot** 图标，双击启动。

### 1.3 首次启动会遇到的系统提示

- **SmartScreen 拦截**：未签名的个人开发者程序会被 Windows SmartScreen 提示。点「更多信息」→「仍要运行」即可。
- **UAC 管理员提权**：DiskPilot 需要管理员权限才能直读 NTFS MFT（2–5 秒扫完整个 C 盘）。安装包带 manifest，启动时会自动弹一次 UAC，点「是」。如果不想每次提权，可用普通权限运行——扫描会自动降级为逐目录遍历，只是慢一些，其余功能不受影响。

### 1.4 版本更新

设置 → 通用 →「版本更新」：点击「检查更新」对比 GitHub Releases。发现新版本会显示版本号与更新说明，点「前往下载页」跳转下载。**更新只会提示，不会静默改动你的文件。**

---

## 二、免责声明（首次启动必看）

**首次启动会强制显示免责声明弹窗**，请务必完整阅读：

- 弹窗包含 **10 秒倒计时**，倒计时结束前「同意」按钮不可用。
- 必须**滚动到最底部**，「我同意以上声明，开始使用」按钮才会点亮。
- 点「我同意」后进入主界面；**之后再次打开软件不再弹出**（记住你的选择）。

> 该弹窗在每次全新安装 / 清除应用数据后都会再次显示。

免责声明关键内容摘要（全文见[第二十二章](#二十二免责声明全文)）：

- **作者**：涙不再为你而流（QQ 3167245951 · 3167245951@qq.com · [github.com/lxfebd](https://github.com/lxfebd)）
- **软件性质**：免费开源的个人维护软件，按「现状」提供，不附带任何明示或默示担保。
- **数据隐私**：无账号、无遥测、无云存储；唯一对外请求是你自行配置的 AI 服务商与 MCP 服务器。
- **清理安全**：所有清理必须先出清单 + 用户确认；默认进系统回收站；保护盘根 / 系统目录 / 用户主目录。
- **风险责任**：使用本软件进行的任何操作（包括清理、压测、工具调用）均由使用者自行承担后果。

---

## 三、界面总览：三大视图

顶部是**磁盘条**（列出所有硬盘：盘符、总量、已用、剩余、已扫状态）。磁盘条下方有三个视图切换：

| 视图 | 入口 | 干什么 |
|---|---|---|
| **总览** | 顶部分段控件「总览」 | 磁盘资产一屏看全：盘卡、分类占用、Top N、建议清理、空间趋势、红盘救援 |
| **工作台** | 顶部分段控件「工作台」 | 三栏布局：目录树 + AI 聊天 + Studio 清理工作台，深度下钻与清理 |
| **工具墙** | 顶部分段控件「工具墙」 | 图吧工具箱集成 + 网络检测 / 系统信息 / 硬件信息 / 基准测试 / 插件市场 |

---

## 四、磁盘扫描：看清空间都去哪了

### 4.1 怎么扫

1. 点「一键扫描全部硬盘」，或点某个盘卡 → 开始扫描。
2. **Windows 上走 NTFS MFT 直读**：整盘 C: 只要 **2–5 秒**（需要管理员权限）。
3. **有扫描基线时走 USN 增量**：第二次扫描同一块盘时，通过 USN Journal 只扫变化部分，比全量更快。
4. 无 MFT / 无 USN 基线 / 非 NTFS（移动盘、U 盘、网盘）自动降级为**逐目录并行遍历**（jwalk），会慢一些，属正常。

### 4.2 扫描结果去哪了

扫完出现两张图（工作台视图）：

- **左侧树视图**：像资源管理器，每行带占用百分比条，可展开下钻。
- **中间 Treemap**：彩色矩形拼图，占空间大的文件夹画得大块——一眼看出 80GB 的微信文件在哪。

### 4.3 缓存与性能

- 已扫过的盘再点 = **秒开**（走后端内存缓存 `scan_tree`），不会重复遍历。
- 点盘卡右上角刷新角标 = **强制重扫**。
- 再次打开软件直接进「总览」，**不会一开就自动遍历全盘**（省电省 IO，低配机不卡）；总览页的建议清理只在「扫过的盘」上有数据，没扫过就提示你先扫描——它不会为了算建议偷偷去遍历你的盘。

### 4.4 扫描进度

扫描时进度条显示**单调累计**的已扫文件数（不会来回跳），完成后进度条保留；多块盘并行扫描时互不干扰。扫描耗时取决于盘上文件总量，大仓库盘（几百万小文件）会比普通盘慢。

---

## 五、磁盘资产总览

「总览」页 = 磁盘资产一屏看全：

- **磁盘卡片墙**：每块盘一张卡（总占用 / 已用 / 剩余 / 已扫状态 / 使用率圆环）。使用率 ≥ 85% 的盘圆环变红。
- **红盘救援横幅**：选中的盘使用率 ≥ 85% 时，顶部出现救援横幅提示「磁盘快满了」，可一键「查看可清理项」（滚动定位到建议清理卡片）或直接发起扫描。
- **分类占用卡片墙**：按 cache（缓存）/ media（媒体）/ backup（备份）/ envs（环境）聚合的占用卡片——对应清理脚本的分类口径，点卡片定位回工作台对应节点。
- **Top N 大目录**：当前盘最大的几个一级目录，按大小排序，一眼看出空间被谁吃了。
- **建议清理项**：已扫过的盘直接显示「预计可释放 X」（纯内存计算，秒出）；没扫过则提示「扫描后自动计算可回收空间」。
- **磁盘空间趋势**：每次扫描自动记录一块盘的空间快照（只记录大小数字，不记录任何文件内容），多扫几次后总览出现**迷你曲线**——一眼看出每块盘近期是涨了还是瘦了（如「3 天 +20 GB」），判断哪些盘在悄悄变满。

---

## 六、工作台：目录树与 Treemap

三栏布局（左 / 中 / 右）：

- **左栏 · 目录树**：磁盘 → 目录 → 文件层级，每行带占用百分比条；点节点在中间 Treemap 高亮定位；右键/操作可展开下钻；支持搜索过滤（按文件名 / 扩展名）、排序（大小 / 名称 / 路径深度）、Top N 控制（每目录保留文件数上限，设置里可调）。
- **中栏 · Treemap**：彩色矩形拼图可视化，块大小 = 占用大小，色块分类着色；点选大目录后周围块淡出，聚焦到所选目录的下一层。
- **右栏 · AI 聊天 + Studio 清理工作台**：AI 顾问对话区（见第七章）+ 按软件分组的清理工作台（见第八章）。

---

## 七、AI 顾问：拖文件夹给 AI 问

### 7.1 配置 AI（BYOK，自带密钥）

设置 →「AI 顾问」标签：

1. **接口地址（Base URL）**：填服务商地址。OpenAI、DeepSeek、Kimi、各种中转都直接填就能用；本地 Ollama 不用密钥。
2. **API 密钥**：只存本机（localStorage），永不上传。
3. **模型名称**：如 `gpt-4o-mini` / `deepseek-chat` / `claude-haiku-4-5` 等。
4. 填完点「测试连接」，返回成功提示即可使用。

支持的服务商：Anthropic / OpenAI（及 OpenAI 兼容中转）/ Gemini / Ollama（本地免费，数据不出本机）。填 Base URL 后自动识别协议，也可手动指定。

> **本机性能建议**：设置页会探测本机配置（后端真实探针或浏览器近似值），给出「建议用云端 API」还是「可放心用本地 Ollama」的提示；弱机坚持用本地大模型会有提示与建议。

### 7.2 问 AI

1. 把不认识的文件夹**拖进中间聊天框**（或从左边树里拖）。
2. AI 用人话告诉你：这是哪个软件的数据、删了会丢什么、能不能删、用什么方式删。
3. AI 回复里带「清理建议」按钮时，点它生成**待确认清单**——逐项勾选确认后才会执行，**AI 没有直接删你东西的能力**。

### 7.3 通用设置（设置 → 通用 → AI 组）

- **让 AI 联网**：开启后 AI 可以搜索网页、核实事实再回答。
- **显示思考过程**：展示 AI 的推理、工具调用与检索结果（可折叠）。

### 7.4 隐私承诺（重要）

AI 只收到目录**元数据**：

- 路径名（你的主目录路径会替换成 `$HOME`，脱敏）
- 大小、文件数、扩展名占比
- ≤20 条样本路径

**永远不读文件内容**（聊天记录、图片、文档都不看）。本地 Ollama 更是完全不上网。

---

## 八、Studio 清理工作台：一条一条删干净

右侧「Studio 清理工作台」按**软件**列出可清理项。内置 18 份清理脚本（scaffold）、90 个清理项（scope），覆盖：微信（3.x / 4.x 双版本，23 个清理项）、QQ、Discord、Zoom、Teams、Conda、浏览器缓存、开发缓存、Firefox、Steam 着色器、崩溃转储、系统临时、Docker Buildx、HuggingFace、OBS、IDE 缓存、游戏引擎缓存、360 安全全家桶缓存等。

### 8.1 清理流程

1. 展开某个软件卡片 → 看到分类清理项（缓存 / 媒体 / 备份 / 环境），每项显示预计释放空间与文件数。
2. 勾选要清理的项 → 点清理 → **预览弹窗**（列出将删除的路径样本 + 总文件数 + 总大小）。
3. **两步确认**：点「确认删除」进入预备状态，**5 秒内再点一次「再点真删」** 才执行；5 秒不点自动取消。
4. 删除默认进**系统回收站**——删错了到回收站还原，或在「最近清理」面板一键撤销。

### 8.2 特殊清理项

- **微信清理**：自动检测微信 3.x / 4.x 版本，按账号（wxid_*）筛选要清理的账号目录，避免误清其他账号数据。
- **Conda**：按环境（env）选择要清理的缓存，而不是整包删除。
- **保留天数（days prompt）**：部分清理项支持自定义「保留最近 N 天」——如「只删 30 天前的缓存」，天数按项记忆，跨会话保留。

### 8.3 三种执行模式

| 模式 | 含义 | 何时用 |
|---|---|---|
| **recycle** | 移入系统回收站（默认） | 默认选择，可还原 |
| **quarantine** | 移入 7 天隔离区 | 可选保险，7 天后可一键还原 |
| **delete** | 直接删除 | **只在用户显式选择时用**，不可恢复 |

> 不是每个文件夹都有清理脚本。陌生的大文件夹先用 AI 问，问清楚再决定。**宁可错放 1000GB，不可错删一个文件。**

---

## 九、清理确认与撤销：安全执行链路

### 9.1 预览弹窗（每次清理必看）

- 列出**将删除路径的样本**（超出显示上限会提示「还有 N 个未列出」）。
- 显示总文件数与总大小。
- 明确提示「进系统回收站，可右键还原」「回收站默认 30 天后自动清空」。

### 9.2 后端保护（不可绕过）

`protected_path` 守卫在后端拦截危险目标：**盘根、系统目录（Windows/System32 等）、用户主目录根**直接拒绝执行。清理脚本本身还有红线校验（glob 不允许命中数据库文件、聊天记录、账号状态、用户收藏、加密物料等）。

### 9.3 最近清理（Undo 面板）

每一次操作都追加写 `undo.jsonl` 日志（可追溯「什么时候删了什么」）。「最近清理」面板按时间倒序列出：

- **隔离（quarantine）** 记录可一键「恢复」。
- 回收站 / 删除记录透明展示（回收站文件从系统回收站还原；直删的按设计不可恢复）。

### 9.4 高危 AI 清理提案

AI 生成的清理建议（`CleanupProposalDialog`）：

- 逐项展示：路径、是什么、干什么用、删了会怎样、风险等级（低危 / 高危）。
- **高危项必须手动输入「确认」二字才能勾选**。
- 最后点「确认」后只执行勾选项，经后端 `execute_ai_plan`（user_confirmed=true）执行。

---

## 十、工具墙：图吧工具箱集成

顶部切到「工具墙」——集成图吧工具箱（绿色版）的 30+ 真实可执行工具。

### 10.1 首次使用：设置工具目录

工具墙默认扫描「图吧工具箱」的 Tools 目录。找不到时会提示你填写路径（如 `D:\图吧工具箱\Tools`），填完保存即刷新。

### 10.2 两种视图

- **CLI 直通**：文档驱动的命令行工具清单（30 个 manifest，参数表/用法示例/风险级别/权限级别），显示「N/M 就位」（缺文件标红）。
- **全部工具**：全目录 12 分类图标墙。

### 10.3 12 分类导航

硬件信息 / CPU工具 / 主板工具 / 内存工具 / 显卡工具 / 磁盘工具 / 屏幕工具 / 综合工具 / 外设工具 / 烤鸡工具 / 游戏工具 / 其他工具。

- **单击工具卡** = 看详情（说明、参数、示例、风险级别、权限级别）。
- **双击工具卡** = 启动工具。
- **含广告工具提示**：DDU、CoreTemp、Geek Uninstaller 等免费版自带广告/赞助的第三方工具，卡片打「含推广」徽标，启动确认文案里明确提示，避免毫无防备看到广告。
- **移除工具**：综合工具/低风险分类里不需要的工具，详情卡有「移除此工具」——会把它**移进回收站**（不是直删），可在「最近清理」里恢复。

### 10.4 启动安全

- **高风险工具**启动前先弹确认。
- **需要管理员权限**的工具（提示 os error 740）自动弹 UAC 提权。
- **L3 永久禁止**的工具（格式化、刷 BIOS、超频、删系统文件）直接拦截不可启动。
- 所有启动都用 `CREATE_NO_WINDOW` 避免黑框闪烁；GUI 工具启动即返回，不阻塞界面。

### 10.5 内置服务工具（工具墙顶部一行）

| 服务 | 说明 |
|---|---|
| 工具墙 | 12 分类图标墙（当前页） |
| 网络检测 | 后端真实 TCP 连接检测百度 / 必应 / GitHub 连通性，快速判断是不是网络问题 |
| 系统信息 | 操作系统版本 / CPU 核心数 / 实时占用 / 内存 / 开机时长，5 秒轮询（切后台自动暂停） |
| 硬件信息 | CPU / 显卡 / 内存 / 磁盘 / 温度等硬件聚合（见第十三章） |
| 基准测试 | CPU / 内存 / 磁盘基准 + 稳定性压测 + GPU 甜甜圈（见第十二章） |
| 插件市场 | 内置工具插件化 + 社区插件（见第十七章） |

---

## 十一、服务工具行：网络 / 系统信息 / 硬件信息

### 11.1 网络检测

点「开始检测」，后端用真实 TCP 连接（不走浏览器跨域限制）检测三个常用站点：百度、必应、GitHub。每个站点显示 可达 / 不可达 + 延迟毫秒。**用途**：电脑「上不了网」时快速区分是断网、DNS 问题还是特定站点被墙。

### 11.2 系统信息

只读展示：操作系统版本、CPU 核心数、CPU 实时占用、内存总量与占用率、开机时长。每 5 秒刷新一次；窗口最小化/切后台时自动暂停轮询，恢复可见立即刷新——后台零占用。

### 11.3 硬件信息

见第十三章（硬件中心）。

---

## 十二、基准测试与压力测试（AIDA64 复刻）

「基准测试」页直跑 agent-server 基准工具（CPU / 内存 / 磁盘 / 压测 / 体检报告），结果实时展示并保存**最近 12 次历史对比**。

### 12.1 基准项

| 基准 | 对标 | 说明 |
|---|---|---|
| **CPU 基准** | CPU-Z Benchmark | 多线程整数质数筛 + 浮点 π 级数，纯计算只读；受睿频/散热影响，多次取中位更稳 |
| **内存基准** | — | 读 / 写 / 复制带宽 + 指针追逐延迟（64MB 缓冲避免全缓存命中） |
| **磁盘基准** | — | 顺序 / 随机读写，用临时文件测后即删；默认 dry-run 只出计划，可改参数真跑（如 `size_mb`、`dry_run=false`） |
| **GPU 信息** | — | 显卡型号 / 显存 / 驱动 / 分辨率 / 温度快照（深度 3D 跑分见 GPU 甜甜圈） |

> bench_disk 运行时界面显示「运行中…」+ 计时（已运行 Ns），不是卡死：后端真实运行一般数秒到十几秒（取决于参数）。写参数后记得把 `dry_run` 改 `false` 才真跑。

### 12.2 稳定性压测（CPU · 对标 CPU-Z Stress CPU）

- CPU 满载 + **每秒温度采样**，实时渲染温度曲线。
- **超 95℃ 自动熔断停止**（写操作 L1，需确认；无温度源时如实提示「无熔断护栏」）。
- 「停止压测」按钮跨进程发送停止信号，压测立即退出。

### 12.3 GPU 甜甜圈压测（对标 FurMark 甜甜圈）

- WebGL 全屏渲染循环（旋转甜甜圈 + 背景粒子 + 闪烁核心），让 GPU **真实满载**（对标 FurMark）。
- 实时统计**真实 FPS**（帧差/秒，FurMark 同口径）与时长。
- 运行中每秒轮询 GPU 温度，**超 90℃ 自动熔断**停止。
- 两步确认（写操作）后进入全屏渲染层；随时可退出。

### 12.4 历史

每类基准独立保存最近 12 次记录（localStorage），可折叠查看历史结果对比。

---

## 十三、硬件中心：报告与历史对比

「工具墙 → 硬件信息」聚合页：

### 13.1 硬件信息

- **CPU**：型号 / 核心线程 / 频率 / 实时占用 / 温度
- **GPU**：型号 / 显存 / 驱动 / 分辨率 / 实时占用 / 温度（NVIDIA 走 nvidia-smi，AMD 走 ADL，普通权限可读）
- **内存**：总量 / 占用 / 频率
- **主板 / BIOS**：型号 / 固件版本
- **磁盘 SMART**：通电时间、温度、磨损、读写错误——一眼看出硬盘健康度
- **温度传感器**：自研 SuperIO 直读（inpoutx64，普通权限真读 CPU/主板温度），回退链 HWiNFO → fancmd → ACPI/GPU/SMART

### 13.2 报告生成

可生成 **Markdown / JSON / HTML** 三种格式报告。导出到文件（L1，默认开）。

### 13.3 历史快照与对比

- 每次生成报告自动归档一份快照（`hw-history/`，最多保留最近 **30 份**）。
- 「硬件历史快照」区可任选**两次归档并排对比**：温度 / SMART 磨损 / 通电时长变化——判断硬件是否在加速老化。
- 对比**只读已有归档，不会重新采集**；只读查询不触发硬件采集。

### 13.4 压力测试（烤机）

CPU（Prime95）/ GPU（FurMark）/ AIDA64 基准随时可跑、随时可停（「停止」按钮跨进程生效）。启动前有风险提示（风扇高速运转、确保散热正常、笔记本建议接电源）。温度熔断（95℃ CPU / 90℃ GPU）自动保护。

### 13.5 风扇控制

见第十四章。

---

## 十四、系统工具：启动项 / 风扇 / 驱动

设置 →「系统工具」标签，三个能力都是 L2（权限中心开关 + 每次执行确认）：

### 14.1 启动项管理

枚举注册表 Run 键（HKCU / HKLM）与启动文件夹（用户 / 机器），显示启动项详情：

- **禁用**：生成 `.disabled` 备份后再禁用（可逆）。
- **删除**：两步确认。
- 后台监听启动项变化，实时刷新。

### 14.2 风扇曲线建议

读温度 / 负载传感器，生成散热档位与建议曲线锚点（静音 / 均衡 / 高性能 / 极速），按使用场景给建议（游戏 / 待机 / 满载）。

### 14.3 风扇控制（真写控）

- 多通道探测：内置 WMI + 厂商 / 第三方 CLI 通道（如 NoteBook FanControl 的 nbfc.exe），都不支持时**如实提示**并给出安装建议，不写假数据。
- 支持档位调速（静音 / 均衡 / 高性能 / 极速）或自定义曲线应用（inpoutx64 直读 IT8689E SuperIO，40% / 80% 双向可控）。
- 应用后**持续监控温度，超 90℃ 自动回退原转速**（温度熔断守护）。
- 每次执行需确认（L2）；退出/还原由状态文件持久化交还主板控制。

### 14.4 驱动更新检查

枚举已装驱动 + 查询 Windows Update 可选驱动更新（联网），列出可更新项。

---

## 十五、权限中心：四级权限模型

设置 →「AI 权限中心」标签。**控制的是「AI 能不能调用某个工具」**，L1/L2 即使开启，每次执行仍在确认面板里单独确认一次。

| 级别 | 含义 | 默认 |
|---|---|---|
| **L0 只读** | 读硬件信息 / 硬盘健康 / 生成报告。零风险，随时可调 | 始终开启，**不可关闭** |
| **L1 受控** | 压力测试 / 导出报告 / 运行 CLI 工具 | 默认开，每次执行需确认；可设「本次会话免确认」 |
| **L2 高危** | 调整电源计划 / 执行清理 / 管理启动项 / 风扇 / 驱动 | 默认关，需手动开启，每次仍需确认 |
| **L3 永久禁止** | 格式化 / 刷 BIOS / 超频 / 删系统文件 | **不可解锁**（没有锁孔） |

### 权限清单（22 项）

**L0（只读，恒开）**
- 读取硬件信息（CPU / GPU / 内存 / 主板 / BIOS / 磁盘 / 温度）
- 读取硬盘健康（SMART 通电时间 / 温度 / 磨损 / 读写错误）
- 生成硬件报告（markdown / json / html）

**L1（受控，默认开 + 确认）**
- 压力测试（Prime95 / FurMark / AIDA64 烤机与基准）
- 导出报告到文件
- 运行工具箱 CLI 工具

**L2（高危，默认关 + 每次确认）**
- 调整电源计划
- 执行清理计划（按 AI 清单回收文件）
- 管理插件（安装 / 卸载 / 插件化，写 Tools 目录）
- 管理社区清理脚本（安装前做 id 校验 + 红线校验 + Ed25519 签名强制）
- 管理启动项（注册表 Run 键与启动文件夹）
- 风扇曲线建议 / 风扇控制（超温自动回退）
- 驱动更新检查（联网）
- 管理 MCP 服务器（声明为可写的服务器调用工具每次需确认）
- 控制系统状态（结束进程 / 启停服务 / 启动程序 / 管理计划任务；系统关键进程与服务会被守卫拒绝）
- 回收文件到回收站（file_recycle，可逆；盘根 / 系统目录被守卫拒绝）
- 卸载软件（uninstall_app，只允许 UninstallString 白名单静默参数）

**L3（永久禁止，不可解锁）**
- 格式化 / 整盘覆写（diskpart / 格式化 / Ventoy 写盘）
- BIOS 刷写（FPT64 等固件刷写）
- 超频 / 电压调节
- 删除系统关键文件（Windows / System32 等系统目录写入）

> 设计理念：**用户拥有「给钥匙」的权利，但有些门没有锁孔**——L3 就算开关状态被改了，`isPermEnabled` 也永远返回 false。这是保护用户，不是不信任用户。

---

## 十六、MCP 服务器：接入任意 MCP 服务

DiskPilot 支持添加**标准 MCP 服务器**（本地 stdio 进程或远程 streamable HTTP），其暴露的工具自动注册进 AI 工具表——AI 对话时可直接调用（文件系统、浏览器、数据库、各类业务系统的 MCP 服务）。

### 16.1 怎么加（设置 → MCP 标签 → 添加服务器）

**本地 stdio 进程**：
- 填启动命令（如 `npx -y @modelcontextprotocol/server-filesystem`）
- 可加参数、环境变量、工作目录

**远程 https 地址**：
- 填 MCP streamable HTTP 地址（**强制 https，http 一律拒绝**）
- 可加请求头与超时

### 16.2 服务器配置项

- **启用**：停用 = 不接入 AI。
- **声明为可写**：见下「安全模型」。
- **工具前缀**：防多台服务器工具重名（如填 `myfs` 则工具名变成 `myfs_xxx`）。
- **测试**：添加后可看它暴露了哪些工具、是否连得上。

### 16.3 安全模型

- 添加 / 编辑 / 删除服务器需要「管理 MCP 服务器」权限（`mcp.manage`，L2，默认关）。
- 默认按**只读 L0** 处理——AI 调用该服务器工具不需要额外确认。
- 服务器若可能暴露写操作（删文件 / 移动 / 写库等），务必勾「声明为可写」——勾选后 AI 每次调用它的工具都会弹确认卡；未开启 `mcp.manage` 权限则直接拒绝。
- 远程地址强制 https；连接失败不影响使用（失败服务器自动跳过，工具表里只剩连得上的）。
- 服务器配置只存在你本机（`%APPDATA%/dev.diskpilot.app/mcp-servers.json`）。

> 提示：与「工具墙」的本地插件（tool.plugin.json + 签名）是两套机制——MCP 走通用协议，插件是给工具墙加 GUI 工具的独立体系。

---

## 十七、插件市场：内置插件化 + 社区插件

工具墙 →「插件市场」（或设置 → 插件市场标签）。两个 tab：

### 17.1 内置工具插件化

给 Tools 里已有的工具补上 `tool.plugin.json`，把它们变成**可管理的插件**（工具墙显示「插件」徽标、权限声明、AI 可列出/卸载）。**工具本体不下载不移动**——只补元数据。

- 每个内置工具条目显示：名称 / 简介 / 分类 / 风险（低危/中危/高危）/ 权限级别 / 出品者。
- 点「插件化」给该工具生成插件清单；已插件化的点「摘除插件身份」。
- **远程安装**：粘贴插件 zip 的 https URL，下载后自动解压到 Tools 根对应分类（写操作，走「管理插件」权限）。

### 17.2 社区插件

配置官方 / 社区**索引 URL** 后一键拉取插件：

- **安全强制**：远程下载强制 https + **Ed25519 签名校验**；安装走「管理插件」权限（每次需确认）。
- **索引 URL 默认留空** = 未配置索引（只显示内置占位条目）。填入地址 →「保存 URL」→「刷新索引」。
- 「校验」检查索引签名与每个插件的字段完备性。
- **已安装台账**：可查看已装插件，支持**更新 / 回滚**（blob 缓存命中可离线更新）。
- 索引与下载包缓存在 `app_data_dir`（plugins-registry.json / plugin-cache/）。
- 刷新失败保留旧索引，不影响已装插件。

---

## 十八、清理提醒：后台只读巡检

设置 →「清理提醒」标签：

- **定时检查可清理空间**：按设定间隔在后台跑一次扫描建议（**只读统计，绝不自动删除**——清理仍由你在总览页逐项确认后进回收站）。
- **检查间隔**：6 小时 / 12 小时 / 24 小时 / 3 天。
- **提醒阈值**：可释放超过该量才提醒（可调）。
- **手动检查一次**：点按钮立即跑一次检查，返回可清项列表。
- 有值得清理的项时弹系统通知；未开启或未达阈值则提示「本次检查无可清项或提醒未开启」。

> 每次检查会做一次扫描统计，可能持续数十秒，间隔太短会占用磁盘 IO。

---

## 十九、设置：全部八个标签页

右上角 ⚙ 打开设置：

### 19.1 外观（appearance）

- **语言**：跟随系统 / 中文 / English（切换立即生效，不用重启）。
- **主题风格**：17 套主题预设（Vercel 技术风 / Minimalist 极简 / 豆包 Docs / Claude 暖纸 / Google 清新 / 火山引擎 / Nerv 霓虹 / TRAE 工坊 / Motion Fit 动感 / Barbie 芭比 / Apple 果味 / Golden Time 鎏金 / Vibe Camp 露营 / 21st 石墨 / Steam 蒸汽 / 深色（Nerv）/ 专业工具风）。
- **主题颜色**：自动（跟随主题默认色）或自定义主色（每套主题各几个品牌主色色板，点一下即换 accent 家族）。
- **字号**：偏小 / 标准 / 偏大 / 特大，或滑条 80%–130%，立即生效。
- 改完立刻生效，不用重启。

### 19.2 AI 顾问（ai）

见第七章。含：本机性能档位提示、Base URL、API 密钥（只存本机）、模型名称、测试连接、保存 / 清除。

### 19.3 通用（general）

**AI 组**
- 让 AI 联网（AI 可搜索网页核实事实）
- 显示思考过程（AI 的推理、工具调用与检索结果，可折叠）

**性能组**
- 并行扫盘：全盘扫描时同时扫多个磁盘，更快（默认开）
- 硬件加速（GPU）：用显卡渲染界面，滚动与大树更流畅；**改动重启后生效**。若界面卡顿、鼠标迟缓，确认此开关保持开启（关闭后走软件渲染，滚动明显掉帧）
- 每目录保留文件数：100 / 300 / 500 / 1000——文件越多扫描结果越完整，内存占用越高，下次扫描生效

**版本更新**
- 检查更新：对比 GitHub Releases，发现新版显示版本号与说明，跳转下载页（不会静默改动文件）

> AI 未配置时，联网与思考开关暂不生效——先到「AI 顾问」标签填好接口。

### 19.4 AI 权限中心（perms）

见第十五章。四级权限模型 + 22 项权限开关 +「本次会话免确认」开关。

### 19.5 系统工具（system）

见第十四章。启动项管理 / 风扇曲线建议 / 风扇控制 / 驱动更新检查（均 L2）。

### 19.6 MCP（mcp）

见第十六章。添加 / 编辑 / 删除 / 测试 MCP 服务器，声明可写，工具前缀。

### 19.7 插件市场（market）

见第十七章。内置工具插件化 + 社区插件注册表。

### 19.8 清理提醒（reminder）

见第十八章。开关 / 间隔 / 阈值 / 手动检查。

---

## 二十、数据存哪与隐私承诺

### 20.1 数据存储位置

| 数据 | 位置 |
|---|---|
| 撤销日志（undo.jsonl）| `%APPDATA%/dev.diskpilot.app/` |
| 隔离区 | `%APPDATA%/dev.diskpilot.app/`（可选，7 天） |
| MCP 服务器配置 | `%APPDATA%/dev.diskpilot.app/mcp-servers.json` |
| 硬件历史快照 | `%APPDATA%/dev.diskpilot.app/hw-history/`（最多 30 份） |
| 磁盘空间趋势 | `%APPDATA%/dev.diskpilot.app/space-history.jsonl` |
| 设置（主题 / AI 配置 / 权限）| 浏览器 localStorage |
| 基准历史（每类 12 条）| localStorage |

### 20.2 隐私承诺

- 无账号、无遥测、无云存储、无云上传。
- 唯一对外请求是你**自行配置**的 AI 服务商与 MCP 服务器（还有可选的检查更新 / 驱动更新检查）。
- AI 只收目录元数据，永远不读文件内容；本地 Ollama 完全不上网。
- 枚举用户数据目录只列文件夹名，绝不读取聊天数据库 / 媒体文件 / 账号密钥。

---

## 二十一、常见问题（FAQ）

**Q：扫描时看到 MFT 失败的提示？**
正常。无管理员权限或盘不是 NTFS 时自动用逐目录模式，只是慢一点，不影响使用。

**Q：启动工具提示需要管理员权限 / UAC 取消？**
工具本身要求管理员。第一次点启动会弹 UAC，点「是」继续；点「否」就是取消，不会启动。

**Q：清错了能找回吗？**
默认进回收站，回收站还原或「最近清理」撤销即可。隔离区模式 7 天内可一键还原。直删（delete）不可恢复——所以默认永远是回收站。

**Q：AI 会看到我的文件吗？**
不会。只发元数据（路径脱敏为 `$HOME`），永远不读文件内容；本地 Ollama 更是完全不上网。

**Q：低配机卡吗？**
不卡了。应用启动不会自动遍历磁盘（总览页的建议清理只在扫过盘后才有），你点扫描才扫。硬件加速记得保持开启。

**Q：bench_disk / 基准测试点了半天没反应？**
不是卡死。基准工具后端真实运行需要数秒到十几秒（磁盘基准默认 dry-run 只出计划；改 `dry_run=false` + `size_mb` 才会真跑）。界面有「运行中…」+ 计时（已运行 Ns）。

**Q：清理提醒弹通知说要清理，会不会自动删？**
不会。提醒只出清单，清理仍需你在总览页逐项确认后进回收站。

**Q：工具墙里没有工具？**
需要「图吧工具箱」绿色版的 Tools 目录，在工具墙顶部填路径（如 `D:\图吧工具箱\Tools`）即可。云端下载的只是工具清单，不含绿色包本体。

**Q：风扇控制不生效？**
先确认主板/笔记本的 SuperIO 芯片是否被识别（自研直读支持 IT8689E 等）；多通道探测会如实提示可用通道，都不支持时给出安装建议（如 nbfc.exe），不写假数据。

**Q：MCP 服务器加了连不上？**
远程地址强制 https（http 一律拒绝）；连接失败的服务器自动跳过，工具表里只剩连得上的，不影响使用。

**Q：怎么彻底不用免责声明弹窗？**
它只在首次启动显示一次，之后不再弹出；卸载重装或清除应用数据后会再次显示（这是安全设计）。

---

## 二十二、免责声明全文

以下为软件首次启动时展示的免责声明完整内容：

### 使用前必读 · 免责声明

**作者信息**

- **作者**：涙不再为你而流
- **QQ**：3167245951
- **邮箱**：[3167245951@qq.com](mailto:3167245951@qq.com)
- **GitHub**：[https://github.com/lxfebd](https://github.com/lxfebd)

**一、软件性质与用途**

DiskPilot 是一款面向个人用户的 Windows 磁盘扫描与清理辅助工具。本软件基于 Tauri 2 + Rust + React 开发，主要功能包括：磁盘占用扫描、文件归类展示、按应用清理缓存（scaffold 机制）、AI 辅助分析（需用户自行配置 AI 服务商 API Key，BYOK 模式）。

**二、数据与隐私**

本软件**不收集、不上传、不存储**任何个人数据：无遥测、无账号系统、无云存储。所有扫描结果、清理记录、配置数据仅保存在本机。唯一可能的网络请求来自你主动配置的 AI 服务商接口——你发送的目录元数据（仅路径与大小，不含文件内容）会按你选择的厂商、以你配置的 Key 发送，用于生成分析建议。请自行评估所分析目录的敏感程度。

**三、清理行为与安全边界**

本软件遵循「宁可错放 1000GB，不可错删一个文件」的安全原则：

1. 所有清理操作**先出清单、经你确认**后才执行，绝不自动清理；
2. 清理默认进入**系统回收站**，可随时还原；「彻底删除」仅在你显式选择时使用；
3. 扫描/清理/基准测试均**不触碰**系统目录、数据库文件、聊天记录、账号状态、加密物料等受保护路径；
4. 压测类功能（CPU/GPU/甜甜圈）会显著占用资源并升温，请确认散热可靠后再运行。

**四、风险与责任声明**

本软件按「现状」提供，作者尽力保证可靠与安全，但**不承担**因以下情形导致的任何直接、间接或连带损失：

1. 因用户误操作、未仔细确认清理清单、或擅自修改配置导致的文件误删；
2. 因第三方 AI 服务（模型输出、接口故障、服务中断、隐私风险）引发的任何问题；
3. 因硬件超频/压测/温度过高导致的硬件损伤；
4. 因系统环境差异（驱动、权限、杀软拦截、Windows 更新）导致的功能异常；
5. 在商用/关键业务环境使用本软件造成的业务损失——本软件仅面向个人非商用场景。

本软件为**免费开源**项目，无任何商业承诺与售后义务。使用即视为已阅读并同意本声明全部条款。

**五、开源许可与第三方声明**

本软件以开源形式发布，遵循仓库内声明的许可协议。软件集成的第三方工具（如图吧工具箱内各硬件工具）版权归各自作者所有，仅作本机调用，不修改、不分发其二进制。若第三方工具侵犯你的权益，请通过上方联系方式联系作者处理。

---

*说明书版本：v0.2.0 · 配套软件版本 0.2.0 · 如有疑问联系作者 QQ 3167245951*
